export interface ItableData {
    id: number,
    date: string,
    name: string,
    count: number,
    distance: number
}

export interface ItableHead {
    id: string,
    text: string,
    isActive: boolean
}