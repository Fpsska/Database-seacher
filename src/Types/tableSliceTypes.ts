export interface ItableData {
    id: number,
    date: string,
    name: string,
    count: number
    distance: number
}

export interface ItableHead {
    id: number,
    text: string,
    isActive: boolean
}